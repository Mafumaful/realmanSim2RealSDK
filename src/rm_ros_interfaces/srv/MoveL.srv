# 笛卡尔空间直线运动服务

# 请求
string arm_id                    # 机械臂标识: "arm_a", "arm_b", "arm_s"
geometry_msgs/Pose target_pose   # 目标TCP位姿
float64 speed                    # 速度 (单位: m/s)
bool blocking                    # 是否阻塞等待完成
float64 timeout                  # 超时时间 (单位: 秒)

---

# 响应
bool success                     # 执行结果
string message                   # 结果描述
float64 execution_time           # 执行时间 (单位: 秒)
