# 笛卡尔空间运动动作

# 目标
string arm_id                    # 机械臂标识: "arm_a", "arm_b", "arm_s"
geometry_msgs/Pose target_pose   # 目标TCP位姿
float64 speed                    # 速度 (单位: m/s)
string motion_type               # 运动类型: "linear" 或 "joint"

---

# 结果
bool success                     # 执行结果
float64 execution_time           # 执行时间 (单位: 秒)
string message                   # 结果描述

---

# 反馈
geometry_msgs/Pose current_pose  # 当前TCP位姿
float64 progress                 # 进度 (0.0-1.0)
float64 elapsed_time             # 已用时间 (单位: 秒)
